<%= form_for(@member) do |f| %>
  <% if @member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
      <% @member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="col-md-2 text-right"><%= t('FirstName') %>:
    </div>
    <div class="col-md-1"><b><%= f.text_field :first_name %></b>
    </div>
  </div>
  <div class="row">
    <div class="col-md-2 text-right"><%= t('MidName') %>:
    </div>
    <div class="col-md-1"><b><%= f.text_field :mid_name %></b>
    </div>
  </div>
  <div class="row">
    <div class="col-md-2 text-right"><%= t('LastName') %>:
    </div>
    <div class="col-md-1"><b><%= f.text_field :last_name %></b>
    </div>
  </div>
  <div class="row">
    <div class="col-md-2 text-right"><%= t('Description') %>:
    </div>
    <div class="col-md-1"><b><%= f.text_area :description %></b>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
