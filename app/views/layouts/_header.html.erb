<header>
  <div class="container-fluid">
    <div class="row">      
      <div class="col-md-1">
        <nav>
          <ul>
            <% if user_signed_in? %>
              <li> <%= link_to t('Sign_out'), destroy_user_session_path, method: "delete" %> <%= current_user.login %> </li>
            <% else %>
              <li> <%= link_to t('Sign_in'), user_session_path %></li>
            <% end %>
          </ul>
        </nav>
      </div>
      <div class="col-md-9">
        <h4> Notifications </h4>
      </div>
      <div class="col-md-2">
        <h4> Search </h4>
      </div>
    </div>
<!--    
    </div>
      <div class="row">
      <div class="col-md-9">
        <%= yield(:breadcrums) %>
      </div>
      <div class="col-md-3">
      </div>
    </div>
-->
  </div>
</header>
